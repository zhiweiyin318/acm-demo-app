apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: wordpress
  namespace: wordpress
spec:
  host: wordpress.apps.mc2.dev04.red-chesterfield.com
  port:
    targetPort: 80
  to:
    kind: Service
    name: wordpress
    weight: 100